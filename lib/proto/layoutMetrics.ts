// Shared with app/[locale]/proto/layout.tsx's header/nav heights so Discovery's
// fill-viewport card stack lines up exactly with the fixed chrome around it.
export const HEADER_H = 56;
export const NAV_H = 76;
